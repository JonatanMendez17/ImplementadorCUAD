<Window x:Class="ImplementadorCUAD.ConnectionConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configurar conexión a CUAD"
        Height="260"
        Width="640"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        MinWidth="520"
        MinHeight="220">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="No se pudo conectar a la base de datos CUAD. Ingrese o pegue la cadena de conexión completa que debe utilizarse para leer CUAD."
                   TextWrapping="Wrap"
                   Margin="0,0,0,8" />

        <TextBox x:Name="ConnectionStringTextBox"
                 Grid.Row="1"
                 Margin="0,0,0,8"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="Wrap" />

        <TextBlock x:Name="ErrorTextBlock"
                   Grid.Row="2"
                   Margin="0,0,0,8"
                   Foreground="Red"
                   TextWrapping="Wrap"
                   Visibility="Collapsed" />

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancelar"
                    Width="100"
                    Margin="0,0,8,0"
                    IsCancel="True"
                    Click="OnCancelClick" />
            <Button Content="Probar y guardar"
                    Width="130"
                    IsDefault="True"
                    Click="OnSaveClick" />
        </StackPanel>
    </Grid>
</Window>

